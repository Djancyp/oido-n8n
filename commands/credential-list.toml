name = "credential-list"
description = "List all n8n credentials."

prompt = """
List all n8n credentials using n8n_list_credentials.
Display them in a table showing ID, type, and name.
"""

[parameters]
type = { type = "string", description = "Filter by credential type (e.g. httpBasicAuth, githubApi)", required = false }
