name = "workflow-run"
description = "Get workflow definition and validate it."

prompt = """
Get the full definition of workflow {{id}} using n8n_get_workflow.
Show the workflow name, active status, node count, and connection structure.
"""

[parameters]
id = { type = "string", description = "Workflow ID to inspect", required = true }
