name = "execution-status"
description = "Check the status of a workflow execution."

prompt = """
Get the status of execution {{id}} using the n8n_get_workflow tool on the execution's workflow.
Show the workflow details and current status.
"""

[parameters]
id = { type = "string", description = "Execution ID to check", required = true }
