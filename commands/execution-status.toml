name = "execution-status"
description = "Check the status of a workflow execution."

prompt = """
Get the status of execution {{id}} using the n8n_get_execution tool.
Show the execution status, start time, finish time, and whether it completed successfully.
"""

[parameters]
id = { type = "string", description = "Execution ID to check", required = true }
